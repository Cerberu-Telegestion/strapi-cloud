'use strict';

/**
 * service-terms-version service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::service-terms-version.service-terms-version');
