'use strict';

/**
 * location-external controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::location-external.location-external');
