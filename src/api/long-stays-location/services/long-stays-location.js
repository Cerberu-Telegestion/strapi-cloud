'use strict';

/**
 * long-stays-location service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::long-stays-location.long-stays-location');
