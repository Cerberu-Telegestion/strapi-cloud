'use strict';

/**
 * long-stays-location router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::long-stays-location.long-stays-location');
