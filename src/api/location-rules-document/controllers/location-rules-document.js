'use strict';

/**
 * location-rules-document controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::location-rules-document.location-rules-document');
