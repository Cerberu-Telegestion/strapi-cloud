'use strict';

/**
 * location-rules-document service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::location-rules-document.location-rules-document');
