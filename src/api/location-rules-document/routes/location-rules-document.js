'use strict';

/**
 * location-rules-document router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::location-rules-document.location-rules-document');
