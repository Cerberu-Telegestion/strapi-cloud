'use strict';

/**
 * access-instruction router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::access-instruction.access-instruction');
